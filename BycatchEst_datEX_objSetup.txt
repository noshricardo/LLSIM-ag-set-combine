setupObj<-bycatchSetup(
    modelTry = c("TMBnbinom1","TMBtweedie"),
    obsdat = obsdatExample,
    logdat = logdatExample,
    yearVar = "Year",
    obsEffort = "sampled.sets",
    logEffort = "sets",
    logUnsampledEffort = NA,
    includeObsCatch  = FALSE,
    matchColumn = "Year",
    factorNames = c("Year","EW"),
    randomEffects= NULL,
    randomEffects2= NULL,
    EstimateIndex = TRUE,
    EstimateBycatch = TRUE,
    logNum = NA,
    sampleUnit = "sampled.sets",
    complexModel = formula(y~Year+EW),
    simpleModel = formula(y~Year),
    indexModel = formula(y~Year),
    designMethods =c("Ratio","Delta"),
    designVars=c("Year","EW"),
    designPooling = FALSE,
    minStrataUnit=1,
    baseDir = getwd(),
    runName = "LLSIMBUMtripExample",
    runDescription = "LLSIm BUM by trip, with 5% observer coverage including observed catch in totals",
    common = c("things")[1],
    sp = c("ThingName")[1],
    obsCatch = c("Catch")[1],
    catchUnit = "number",
    catchType = "catch"
)
