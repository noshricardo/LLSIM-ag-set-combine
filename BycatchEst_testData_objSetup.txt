setupObj<-bycatchSetup(
    modelTry = c("Delta-Lognormal", "Delta-Gamma"),
     obsdat = obsDat,
     logdat = logDat,
     yearVar = "year",
     obsEffort = "hooks",
     logEffort = "hooks",
     factorNames = c("Year"),
     randomEffects= NULL,
     randomEffects2= NULL,
     EstimateIndex = FALSE,
     EstimateBycatch = TRUE,
     logNum = NA,
     sampleUnit = "set",
     complexModel = formula(y~Year),
     simpleModel = formula(y~Year),
     indexModel = formula(y~Year),
     runName = "Test Data Example2",
     runDescription = "run with test data",
     common = c("log")[1],
     sp = c("CC")[1],
     obsCatch = c("numb")[1],
     catchUnit = "number",
     catchType = "catch"
 )
